# ROADMAP

## Completed milestones

| Milestone | Commit | Summary |
|---|---|---|
| MVP-001 | `4fdb0ee` | Read-only Kalshi scanner, ranking, API, Compose, tests |
| MVP-002 | `4d1f28a` | Alembic, scanner audit fields, CLI, live tests |
| MVP-003A | `b3ccce2` | Eligibility gate; fixed live payload drift (dollars/fp), MVE filter |
| MVP-003B | `2236323` | Resolution assessments (rule-based judge; LLM behind flag) |
| MVP-003C | `5bcdf48` | Detail enrichment (settlement sources); clarity 0.75→1.00 live |
| MVP-004A | `41b90fd` | Research packets (domains, template collector) |
| MVP-004B | `90f2b62` | Forecast engine, evidence-depth confidence caps |
| MVP-004C | `56f3ab3` | Outcome sync + Brier/log-loss calibration |
| MVP-004D | `0cd62b0` | Scheduled baseline runner + pipeline audit + overlap lock |
| OPS-001 (deploy) | `a562e05` | EVO-X2 deployment: venv + SQLite + user timer |
| OPS-002 | `109d385` | Real-time watcher, price ticks, opportunity signals |
| OPS-003 | `27a4501` / `eeb799d` | Retention/pruning, db-stats, watcher+retention deployed to EVO-X2 |
| OPS-004 | `f9fda96` | Signal promotion + signal-triggered intelligence refresh |
| MVP-004E | `9b46911` | Baseball external research canary (MLB Stats API, source-backed packets) |
| MVP-004F | `20d4fda` | Evidence-aware baseball forecaster (capped non-midpoint forecasts) |
| OPS-005 | `c35e704` | Project canon + agent operating framework; deployed to EVO-X2 with baseball canaries enabled (`71dab1d`) |
| MVP-004G | `918b9de` | Champion/challenger comparison (paired + cohort, sample-size gated) |
| SOCCER-001 | `e1d3b7b` | Soccer/World Cup external research canary (provider-gated, source-backed packets); rolled out to EVO-X2 (`f76baaa`) |
| CRYPTO-001 | `9d72237` | Crypto Arena: read-only Solana memecoin discovery + risk surveillance (DEX Screener, 7 tables, 9 deterministic signal types, CLI/API reports); deployed dark to EVO-X2 (`7606ca6`) |
| OPS-006 | `b0dd1d6` | MarketOps Autopilot: read-only 24/7 coordination (auto-promote/process, crypto scan, sync/score, champion/challenger snapshot, local DB alerts); live on EVO-X2 with 5-min timer (`28b3476`) |
| CRYPTO-002 | `6450194` | Crypto risk engine: heuristics + optional GoPlus/SolanaTracker providers, composite risk scores/levels, activated risk signals, risk reports (read-only risk intelligence — never trade advice); live on EVO-X2 GoPlus-backed (`ad79fde`; SolanaTracker needs an API key — `e2d8ae9`) |
| MEME-RISK-003 | (this) | Holder/sniper/insider/bundler/**creator** risk coverage: new read-only **Birdeye** provider adapter (holder + creator/deployer concentration; `ENABLE_BIRDEYE_RISK`, key optional, payload mapping pending validation, graceful absence); new `creator_concentration` heuristic category (`CRYPTO_RISK_MAX_CREATOR_PCT=15`); explicit provider **coverage** reporting `crypto-provider-health-report` + `meme-risk-coverage-report` + a holder-coverage overlay on `crypto-risk-report` (absence is stated, not silent). Read-only risk intelligence — no EV/trade/sizing/orders/wallets/execution; existing GoPlus/SolanaTracker/MarketOps/EDGE-AUTO/MEME-NEWS behavior unchanged; no migration |
| CRYPTO-TAPE-CADENCE-001 | (this) | Bounded manual crypto tape SESSION helper so repeated CRYPTO-TAPE passes can mature the 15m/1h/6h/24h survival horizons (CRYPTO-RETROSPECT-001's own verdict: provider gaps dominate because horizons lack observations). New `crypto-tape-session --duration-hours N --interval-min N --limit N [--dry-run]`: a fixed, HARD-CAPPED number of derived zero-external-call `run_once` passes in ONE invocation with a sleep between, then exit — NOT a timer, NOT a daemon, never autonomous. Caps: duration ≤36h, interval clamped 15–120 min, ≤144 captures/session; aborts on abnormal pass status or detectable MarketOps error (latest run errored). Dry-run persists NOTHING and never sleeps: prints the planned schedule + expected capture count and runs exactly one dry probe. Session summary: captures planned/run/statuses, per-run row totals (births/snapshots/actors/outcomes), horizon maturity (known/unknown per survived_*), provider-gap share trend across captures (improving/worsening/flat), outcomes tracked/final, db_impact_rows, no-action disclaimer. 15 tests (duration cap, interval clamp both ends, 144-capture bound, dry-run persists-nothing + schedule + single-probe, bounded call/sleep counts, both abort paths, end-to-end summary math incl. birth dedupe across passes, gap-trend math, CLI wiring/rendering, broken-httpx no-network proof, no-daemon vocabulary). Ergonomics only: no flag, no timer, no provider, no budget impact; MarketOps unchanged; no EV/recommendations/sizing/paper/orders/wallets/keys/swaps/signing/execution/autonomy |
| CRYPTO-RETROSPECT-001 | (this) | Read-only retrospective FEATURE/OUTCOME separation analysis over the lifecycle tape: `crypto-retrospect-report --hours N --top N` joins observable features (top10/sniper/insider/bundler/creator concentration buckets anchored to risk-engine thresholds, risk level/score, per-risk-reason cohorts, liquidity depth, volume-to-liquidity shape, boost/attention, social presence, launch venue, graduation, provider coverage, per-missing-info cohorts) to the CRYPTO-TAPE-001 survival outcomes (survived_15m/1h/6h/24h, liquidity_removed, dead_volume, severe_risk, graduated_or_migrated, provider_gap) across the recent token universe — persisted tape birth events preferred as anchors, other tokens derived on the fly from the same persisted rows (never written back). Rates computed over MEASURED outcomes only (immature/gap = unknown, excluded); conservative interpretation ladder `too_thin` (cohort <12) / `provider_gap_dominates` (primary outcome <40% measurable) / `no_separation` (<0.10 delta) / `weak_separator` (>=0.10) / `strong_risk_separator` / `strong_survival_separator` (>=0.25); report ranks best/worst separators, names unreadable dimensions, shows per-cohort examples + thresholds + no-action disclaimer. Compute-on-demand like MEME-SHADOW: NO table/migration/flag/timer, persists nothing (session.new/dirty proven empty), zero external calls, zero SolanaTracker budget impact, MarketOps unchanged. 25 tests (bucketing, row assembly incl. tape-backed flag, unknown-never-in-rates, min-measurable floor, all six interpretation labels, report structure/rendering, strict no-persistence, broken-httpx no-network proof, vocab). MEASUREMENT ONLY: a separation label is feature/label-quality evidence for review triage — no EV/recommendations/sizing/paper/orders/wallets/keys/swaps/signing/execution/autonomy |
| CRYPTO-TAPE-001 | (this) | Read-only Solana memecoin LIFECYCLE TAPE — moves the crypto lane from point-in-time scoring to replayable token lifecycle recording. **Migration 0026**: `crypto_token_lifecycle_runs` / `crypto_token_birth_events` (one per token: first evidence, launch source, first pair/dex, chain-side pair creation, creator address if a provider ever exposed one, mint/freeze authority, metadata/social links, initial price/liquidity/volume/mcap/FDV, bonding-curve state, raw provenance, missing_info) / `crypto_token_lifecycle_snapshots` (per run: market state + top10/sniper/insider/bundler/creator concentration + risk labels + boost/attention/catalyst + pair-count/venue quote quality + per-source provenance ids + provider_coverage + missing_info) / `crypto_token_actor_observations` (creator address/holding, sniper/insider/bundler counts from persisted provider payloads, holder distribution, honest placeholders for first buyers + cohort/cluster refs — public-chain addresses only) / `crypto_token_survival_outcomes` (per birth event, upserted until final: survived_15m/1h/6h/24h, liquidity_removed, dead_volume, severe_risk, graduated_or_migrated, provider_gap; NULL = not yet measurable or gap, never a guess). The tape is **DERIVED**: `crypto-tape-run-once --limit N --hours N [--dry-run]` consolidates rows the existing lanes already persist (crypto tokens/pairs/ticks/discovery events/risk assessments + meme attention/catalysts) — ZERO external calls, ZERO provider-budget impact; dry-run persists nothing; real runs persist ONLY tape rows (proven: no signals, no MarketOps rows). `crypto-tape-report --hours N --top N`: volumes, provider coverage mix, survival label distribution, risk distribution, most-concentrated actor examples, missing-data mix, no-action disclaimer. 35 tests (migration up/down/up, dry-run, tape-rows-only, birth dedupe + field mapping + honest gaps, snapshot consolidation, actor placeholders + cohort counts, per-horizon survival incl. immature/no-observation/final-freeze, provider-gap, report, CLI wiring, broken-httpx no-network proof, vocab). RESEARCH INFRASTRUCTURE ONLY: a survival label is measured token behavior — no EV/recommendations/sizing/paper/orders/wallets/keys/swaps/signing/execution/autonomy; no new flag, no timer, MarketOps unchanged |
| OPS-007 | `a1d4ff6` | Operational hardening: MarketOps overlap guard (skipped/already_running + stale-lock recovery), SQLite busy timeout, DB backup/verify/retention CLI + optional daily timer; deployed + validated live (`19370c2`) |
| MVP-005A-design | `cd6760a` | Edge-precheck design + safety review (`docs/MVP_005A_EDGE_PRECHECK_DESIGN.md`) — gate crossed at paired n=36, d_brier=−0.049, d_log_loss=−0.152 (early_signal) |
| MVP-005A | `1bd134a` | Edge precheck implementation: probability-gap measurement (10 statuses, deterministic precedence, persistence counting), edge_precheck_snapshots audit rows, CLI/API, double-gated MarketOps stage; live on EVO-X2 for manual measurement (`fa0ac34`) |
| MVP-005A.1 | `5324046` | Targeted edge-precheck modes: explicit forecast ids, MarketOps-cycle scoping, recent-refreshed-signals; dedupe window; MarketOps stage now strictly cycle-scoped (broad sweeps stay manual-diagnostic) |
| SOCCER-002 | `2d2cf10` | Soccer evidence-aware forecaster (goal-margin/pace model, red-card + penalty handling, capped ±0.25 shift) — makes soccer forecasts measurable by edge-precheck |
| TENNIS-001 | (this) | Read-only tennis evidence canary: `TennisExternalResearchCollector` (`tennis-external`; template scaffold + set/game/server/winner/surface/rank evidence; provider `template` default, `espn` pending validation) + `TennisEvidenceAwareForecaster` (`tennis_evidence`; match-winner only, midpoint prior, tightly-capped ±0.20 set-margin adjustment, conf cap 0.65, missing-facts→0.50/high-risk) behind `ENABLE_TENNIS_EXTERNAL_RESEARCH`/`ENABLE_TENNIS_EVIDENCE_FORECASTING` (default off). Measurement/research only — no EV/trade/paper/sizing/orders/wallets/execution; baseball/soccer/MarketOps/EDGE-AUTO/meme-news unchanged |
| EVAL-001 | `57e8369` | Frontier evaluation harness: 8 quality sections (signal/forecast/edge/follow-through/microstructure/crypto/latency/safety) + conservative readiness scorecard; evaluation only — readiness labels never authorize live capital |
| OPS-009 | `7746ef9` | Promotion quality: minute-level domain-aware freshness windows + measurement-readiness scoring (market-type/book-quality/source-backed priority; player props deprioritized); promotion stats in run summaries |
| SCANNER-002 / OPS-010 | `00e169b` | Targeted game-level market scan coverage: per-series read-only fetches (`KXWCGAME`/`KXWCTOTAL`/`KXWCSPREAD`/`KXMLBGAME`/`KXMLBTOTAL`/`KXMLBSPREAD`) supplement the generic scan (deduped, partial-failure tolerant); watcher supported-universe supplement (game-level baseball/soccer with two-sided quotes, bounded, never props). Motivated by `docs/VALIDATION_PAR_FRA_2026_07_04.md` |
| MVP-005A.2 / EDGE-AUTO-001 | `134e401` (deploy) | Enabled cycle-scoped edge-precheck inside MarketOps (`MARKETOPS_INCLUDE_EDGE_PRECHECK=true`) after readiness crossed `ready_for_cycle_scoped_edge_automation`; measurement automation only |
| OPS-011 | `dd99146` (deploy) | DB growth/retention observability + alert calibration: `db-growth-report` CLI, retention dry-run detail, configurable warning/critical tiers for DB-growth (1536/3072 MiB) and signal-flood (400/800 per hour) alerts. Ops-only; no alpha/edge/trading change |
| EDGE-ANALYSIS-001 | `d20ca56` (deploy `447e7ae`) | Edge cohort analysis: `edge-cohort-report --hours N` slices watchlist/`paper_candidate_later` snapshots into 10 cohort dimensions (market type/domain/gap sign/abs-gap/confidence/signal type/liquidity/spread/game phase/persistence) and measures per-cohort gap follow-through, labelling each `too_thin`/`promising`/`neutral`/`weak`/`exclude_candidate`; reports whether MVP-005B-design remains blocked. Analysis/reporting only — no advice, no PnL, no flag/logic change |
| MEME-NEWS-002 | (this) | Scheduled always-on read-only meme/news discovery lane: `MemeNewsScoutRunner` wraps the MEME-NEWS-001 scout in a bounded no-raise cycle; `meme-news-run-once [--scheduled]`, `meme-news-report`, `meme-news-alerts`; systemd `probability-arena-meme-news.{service,timer}` (10-min, NOT auto-installed); `ENABLE_MEME_NEWS_SCOUT` (default false) gates only the scheduled path (manual always allowed); local derived alerts (high attention / jump / boost increase / severe risk / provider degradation); `MEME_NEWS_RETENTION_DAYS=14` retention + db-growth line. Read-only discovery only — `attention_score` is not EV/recommendation/instruction; no orders/wallets/keys/swaps/signing/execution/sizing/paper trading; independent of and cannot affect MarketOps/EDGE-AUTO |
| MEME-NEWS-001 | `8239510` (deploy `778469c`) | Read-only meme/news + domain-expansion scout: **Part A** `meme-scan-once`/`meme-scout-report` — DexScreener token-genesis + boost velocity → per-token `attention_score` (freshness/liquidity/volume growth/boost velocity/metadata/social − risk penalty × provider confidence); **Part B** `catalyst-report` — generic source-agnostic `meme_catalyst_events` (dexscreener now; rss/x/discord/telegram placeholders); **Part C** `domain-scout-report` — probability-market inventory grouped by domain/series with `canary_priority` for weather/tennis/basketball/golf/esports/… . 5 audit tables (migration 0019). Discovery/scoring/inventory only — `attention_score` is an interest signal, not a buy/trade/EV score; adds no forecaster; changes no promotion/edge/forecast logic; no EV/paper/sizing/orders/wallets/keys/swaps/signing/execution |
| EDGE-POLICY-001 | `debdfda` (deploy `bbc761d`) | Edge shadow-policy analysis: `edge-policy-report --hours N` simulates 13 candidate cohort filters over existing watchlist/`paper_candidate_later` rows (included/invalid counts, per-horizon follow-through, settlement-conditioned forecast-vs-market Brier on resolved outcomes, `too_thin`/`worse_than_baseline`/`neutral`/`promising_shadow`/`reject_policy` labels) + a decision/gate readout. Read-only shadow analysis — re-slices existing rows only; no live gating, flag, forecaster, edge, or service change; no EV/PnL/trade/recommendation; MVP-005B stays blocked unless a policy clearly clears the gate |
| TENNIS-CAPTURE-SESSION-001 | (this) | Bounded manual tape capture-SESSION helper — repeated captures in ONE invocation, then exit (NOT a timer, NOT a daemon). New `tennis-tape-capture-session --duration-min N --interval-sec N --limit N [--dry-run]` + `run_capture_session`/`summarize_session`: hard caps (duration ≤60 min; interval clamped 30–300s; ≤60 captures/session; provider-call caps inherited from capture_once), count-based loop with sleep between captures, ABORTS immediately on abnormal capture status or a detectable MarketOps error (latest run status=error); session report aggregates captures run, snapshot/link volumes, quote coverage, TOP MOVERS (abs mid range per ticker across the session's runs), provider-call count, DB impact; dry-run passes through (persists nothing → summary honestly unavailable). 12 tests (duration cap, interval clamp, capture-count bound, limit pass-through, both abort paths, dry-run persists nothing, summary movers/coverage/impact, CLI parse/render, vocab; fake recorder — no network). Measurement infrastructure only: no flags, no timers, no forecasts/gates/MarketOps change |
| EDGE-RETIRE-001 | (this) | Formal RETIREMENT of all six EDGE-SELECTION-001 candidates per prereg §6, on the first substantial out-of-sample window (2026-07-11 07:15 UTC, n=297 pure post-lock rows): every candidate `failing_gates` with INVERTED performance (primary `require_gap_follows_move_totals_only` discovery 0.539/+0.42 → validation **0.286/−1.22**; follows-move cohorts closures −1.2..−1.7 vs baseline −0.06) while the NEGATIVE CONTROL (`spread_only`, discovery 0.204/−0.51) posted 0.375/**+0.33** — outperforming every candidate; the cost-adjusted view had independently `cost_killed` every cohort. **Conclusion: the 18-policy search OVERFIT; the locked protocol did its job.** New `docs/EDGE_SELECTION_RETIREMENT_2026_07_10.md` (verdict tables, cost confirmation, control-inversion analysis, standing rules) + `edge-selection-retirement-report` CLI (frozen record + live post-lock re-measurement with [RETIRED] flags) + `RETIRED_CANDIDATES` registry in edge_selection (validation report now annotates retired candidates; protocol statuses untouched). **Standing rules: no retired policy is eligible for live gate/paper/MVP-005B (which remains blocked); resurrection requires a NEW prereg + NEW lock; successor hypotheses should be mechanism-first (e.g. the tennis latency thesis) with cost-adjusted gates from day one.** Registry/documentation only: no behavior/gate/forecast/flag/MarketOps change |
| TENNIS-CANDIDATE-ORDER-001 | (this) | Informative-books-first ordering for bounded tennis capture (measurement selection only — never a value/trade judgment). Measured motivation: alphabetical ordering spent session-1's 40 slots on pre-match Challenger books while MATOCH/IMANAK traded seven figures. New `rank_tennis_candidates` in tennis_watcher: deterministic key = match-winner first → actively-ticking (≤30m) → two-sided quote → 24h volume desc → recent mid movement desc (60m window) → recently source-backed (tape links 24h) → stable ticker sort; per-candidate reason labels (`active_book`/`two_sided`/`high_volume`/`recent_move`/`source_backed`/`fallback_sort`) surfaced as `top_ordering` in scan/capture summaries and CLI output; consumed by `tennis-watch-scan-once`, `tennis-tape-capture-once`, and the watch report's uncovered examples. 10 tests (rank precedence incl. hot-beats-alphabetical, recent-move, stale-not-active, source-backed boost, match-winner precedence, deterministic tie-break, both consumers verified via fake adapters, persists-nothing, vocab). No behavior change outside capture/report ordering; no flags; no forecasts/gates/MarketOps change |
| TENNIS-GOALSERVE-001 | (this) | Goalserve FALLBACK live-state validation (pre-registered plan §7c), built after the decisive API-Tennis failure (REST livescore 0 rows × 25+ probes; WS 0 frames in a 180s in-play probe while 24 Kalshi books moved). New `tennis-goalserve-probe --probes N --interval-sec N --top N` + `GoalserveTennisClient`/`GoalserveValidationService`: bounded repeated fetches of `tennis_scores/live?json=1` (hard caps ≤8 probes/≤10 calls; interval clamped 5–120s); **path-embedded key → URLs never logged/echoed, masked display URL only, failures by exception type**; `normalize_goalserve_live` converts categories/matches/players into the SAME fixture shape the tape linker consumes (dd.mm.yyyy→ISO dates, s1–s5 set scores, game/point score, serve flag, in-play detection excluding Finished/Retired/scheduled-time statuses); matching via the existing `link_candidate` (±1-day, both-code rules); per-probe live/in-play row counts, state-change detection (status/sets/game/point/serve signatures), live-state field availability, matched/miss examples, API-Tennis baseline comparison; verdict ladder `goalserve_pass` (match + in-play + state change) / `goalserve_partial_tune_once` / `goalserve_fail_market_only_or_sportradar` / `insufficient_live_window` / `no_key`, each with a fixed recommendation (pass → TENNIS-TAPE-GOALSERVE-001 or TENNIS-MICROSTRUCTURE-001, explicitly accepted; fail → market-only or Sportradar, stop pushing API-Tennis for live state). New config `GOALSERVE_TENNIS_API_KEY` (default empty; no request without it). 21 tests (normalization incl. in-play ladder + dict-not-list categories, key redaction, no-key no-op, probe cap, source-backed/no-match, live-state fields, state changes, persists-nothing, rendering, vocab incl. markov). VALIDATION ONLY: persists nothing; no models/EV/paper/recommendations/sizing/orders/wallets/keys-committed/signing/swaps/execution/autonomy; no behavior change |
| TENNIS-LIVE-FEED-002 | (this) | Bounded WebSocket LIVE-FEED validation for the measured score-state gap. Tape sessions (25 runs) proved the market half (IMANAK mid 0.245→0.485 in-play at 60s resolution) and disproved REST live state (get_livescore 0 rows × 25 probes; fixtures frozen at "sets 0-0" throughout). New `tennis-api-livefeed-probe --duration-sec N --top N` + `TennisLiveFeedProbe`: one bounded WS connection to the documented `wss://wss.api-tennis.com/live` (key host-only, never printed, never in display URLs; connect errors reported by exception type only; hard duration cap 300s; no reconnects, no timer), frame normalization (object/list/result-wrapper), per-match state-change detection (status/scores/point-count/serve signatures), correlation to current Kalshi candidates via the EXISTING tape linker (±1-day, both-code rules), and a bounded REST comparison (1 livescore + ≤2 fixture dates). Honest verdict ladder: `api_tennis_ws_pass` (events + matches + state changes) / `api_tennis_ws_partial` / `api_tennis_ws_fail_goalserve_next` / `insufficient_live_window` / `no_key`, each with a fixed recommendation; **pre-registered Goalserve fallback plan** (30-day trial, ≤10 calls, same fields/linker, falsifiable 5s point-by-point claim) recorded in the research doc §7c. 19 tests (fake streams; normalization, pass/partial/fail/no-window/no-key verdicts, duration clamp, connection-error key-leak guard with a literal key probe, REST comparison, persists-nothing, rendering, vocab incl. markov). VALIDATION ONLY: persists nothing; no models/EV/paper/recommendations/sizing/orders/wallets/keys-committed/signing/swaps/execution/autonomy; no behavior change |
| TENNIS-TAPE-001 | (this) | Phase 0 read-only SYNCHRONIZED TENNIS TAPE — replayable alignment of API-Tennis score/state with Kalshi tennis market quotes, built on both validated halves (TENNIS-WATCHER-001 market ticks; TENNIS-PROVIDER-001 score source at 73.9% live coverage). **Migration 0025**: `tennis_tape_runs` / `tennis_tape_score_snapshots` (provider event id/date/type/tournament/players/status/state/final+game result/serving/set_scores/missing_info/raw minus point-by-point bulk) / `tennis_tape_market_snapshots` (bid/ask/mid/spread/liquidity/volume) / `tennis_tape_links` (label, basis, player codes, per-side observed_at, score_to_market_delta_s). New `tennis-tape-capture-once --limit N [--dry-run]`: one bounded score pass (HARD CAP 4 provider calls, deduped by date; failures → unresolved, never fabricated) + one chunked quote pass (existing read-only Kalshi adapter, ≤200 tickers) + pure linking (`source_backed_link` both-codes+date; `fuzzy_candidate` single one-sided or ambiguous-exact; `unresolved` unparseable/cap/failed-fetch; `provider_no_match`; `incompatible_market_type`), score snapshots deduped per provider event; dry-run persists NOTHING; not-dry-run persists ONLY tape rows (proven: no ticks/signals/watcher rows). No timer, no scheduled path, no new flags; provider-gap skip when key/provider absent (zero fetches). New `tennis-tape-report --hours N --top N`: runs/volumes/link mix/source_backed rate/quote coverage/in-play count/freshness/mean delta/examples/DB impact + measurement-only disclaimer. 24 tests (migration up/down/up round trip, all five link labels, dry-run, tape-rows-only, score dedup, delta, call cap, provider-gap skip, fail-fetch fallback, key redaction, rendering, vocab incl. markov/odds). MEASUREMENT ONLY: no probability/Markov models, EV, paper, recommendations, sizing, orders, wallets/keys, signing, swaps, execution, autonomy; no forecast/gate/promotion/MarketOps/EDGE-AUTO change |
| TENNIS-PROVIDER-001 | (this) | Tennis live-score PROVIDER research + default-off adapter scaffold. Research (docs/TENNIS_PROVIDER_RESEARCH_2026_07_10.md, official sources cited, fetched 2026-07-10) scored providers against the MEASURED universe (240 live candidates: ~79% ITF-family, ~15% Challenger, main tour a minority): **API-Tennis primary** (documented ATP/WTA/Challenger M+W/ITF M+W coverage, numeric keys + player names + dates → maps onto our validated ticker parse; $40/mo Starter, 14-day trial; risk: coverage-by-plan needs trial verification), **Goalserve fallback** (explicit "all ATP/WTA/Challenger/ITF", point-by-point every 5s, $150/mo, 30-day trial), **Sportradar disqualified for this universe** (ITF World Tennis Tour removed from its Tennis API starting 2025 — official changelog — despite official Challenger coverage), API-SPORTS ruled out (no tennis endpoint), SportMonks ruled out (not an established tennis product), ESPN retired (measured 0/176 + unofficial API). Bounded validation plan: trial key on host .env only (never committed; presence/absence reporting), inline provider env var, ≤10 REST calls, no persistence, decision gates (≥50% source_backed → useful; <25% after one tuning pass → fallback). Scaffold: `ApiTennisFetcher` behind `TENNIS_RESEARCH_PROVIDER=api_tennis` — **no request without `TENNIS_PROVIDER_API_KEY` (default empty)**; adapts get_fixtures → the internal scoreboard shape `_find_event` already matches (documented v1 limitation: 4-letter Kalshi codes don't match; tuning expected). 16 tests (adaptation, state mapping, tour filter, no-key-no-request, secret hygiene, selection wiring, compatibility with existing finder, vocab). RESEARCH/PLUMBING ONLY: no models/EV/paper/recommendations/sizing/orders/wallets/keys-committed/signing/swaps/execution/autonomy; no behavior change; TENNIS-TAPE-001 stays parked |
| TENNIS-WATCHER-001 | (this) | Read-only tennis market TICK coverage — closes the measured market-side gap (tennis markets had zero ticks; not in the watcher universe) so future tape recording/latency studies have market-side data. New `tennis-watch-scan-once --limit N --hours N [--dry-run] [--scheduled]` + `tennis-watch-report --hours N` + `TennisTickWatcher`/`discover_tennis_universe`: DB-only discovery of active tennis markets (recent last_seen, not settled/expired) with series buckets (KXATPCHALLENGERMATCH/KXITFWMATCH/KXITFMATCH/KXATP/KXWTA/KXITF, most-specific first) and tick-covered vs uncovered split; the scan fetches bounded fresh quotes (match-winner first, `TENNIS_TICK_WATCH_LIMIT`=200 default) via the EXISTING read-only Kalshi adapter and records plain `market_price_ticks` rows — same table/shape/retention as the realtime watcher, NO signal detection, NO watcher_runs row, dry-run persists nothing; scheduled path no-ops unless `ENABLE_TENNIS_TICK_WATCHER=true` (default false, no timer installed). NO migration (reuses market_price_ticks + existing raw retention). Validation: real host data (240 active tennis markets, coverage 0.0, series mix ITFMATCH 66/ITF 64/ITFWMATCH 60/CHALLENGER 36/WTA 14) + one real bounded dry-run GET (10/10 fetched 200 OK, 0 persisted, `two_sided_quotes=0` at a no-play hour — honest) + scheduled-guard no-op confirmed. OBSERVATION ONLY: ticks are quotes for research; no EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution/autonomy; no forecast/gate/promotion/MarketOps/EDGE-AUTO behavior change |
| TENNIS-LIVE-SOURCE-001 | (this) | Read-only tennis PROVIDER/SOURCE validation — can persisted tennis markets be mapped to reliable source-backed live match state? New `tennis-live-source-report --top N --hours N` + `TennisLiveSourceReportService`, built entirely on the existing TENNIS-001 scaffolds (`parse_tennis_ticker`, `get_tennis_fetcher`, `_find_event` — nothing new integrated): per persisted tennis market it classifies match_winner/set_winner/prop/unknown (prop markers checked before winner markers so TOTALGAMES ≠ GAME), parses players/tour/date, and attempts provider mapping — `provider_gap` with ZERO fetches under the default template provider; with a configured provider, read-only scoreboard fetches bounded to one per (tour,date) with a hard cap of 6 per run, `source_backed` on event match (+event_status/fetched_at/quote-age/score-to-market-lag), `provider_no_match` when the provider does not cover the event, `stale_provider` warnings on fetch failure. **Live validation (host data, template provider): 1450 persisted tennis markets, 240 live candidates (24h), 1252 match_winner; structural mapping is STRONG — only 4 unparseable tickers and 14 unsplit player codes (~99% parse); every live candidate is a KXATPCHALLENGERMATCH (Challenger tier), consistent with the known ESPN coverage gap.** Conclusion: the blocker is provider event coverage for Challenger/ITF, not ticker/player mapping. VALIDATION ONLY: no probability updates; no EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution/autonomy; changes no MarketOps/EDGE-AUTO/forecast/gate/flag behavior; persists nothing |
| LIVE-MARKET-001 | (this) | Read-only LIVE market/state observation foundation for future in-game probability-market research — observation and latency measurement ONLY. New `live-market-state-report --domain D --top N [--hours N]` + `LiveMarketStateReportService`: live candidates = tickers with ticks in the recency window filtered by domain (reuses `domain_for_ticker`); per market: mid/bid/ask/spread/liquidity from the latest tick, market-update freshness, quote quality (tight/moderate/wide/missing_quotes), volatility diagnostics over 1m/5m/10m windows (window moves, mid range, spread widening, liquidity drift, quote-instability ratio → `volatile_state`/`calm_state`/`insufficient_live_data` — DIAGNOSTIC labels, never signals), and a status ladder (`state_backed_live`/`observable_market_only`/`stale_market_quotes`/`insufficient_live_data`) with `stale_provider` warnings. Tennis match-winner scaffold: participants parsed from persisted market titles (never guessed), set/game/server/match-status extracted from persisted TENNIS-001 research-packet facts when a source_backed packet exists (then score freshness + score-to-market lag + moved-since-score become measurable); otherwise honest `template_only` + explicit `provider_gap` (no validated live tennis score feed — TENNIS_RESEARCH_PROVIDER default template, ESPN mapping unvalidated) with per-field `missing_info`. **Compute-on-demand: NO migration, NO new flags, NO external calls, nothing persisted, nothing fabricated.** Validation on live host data: sports_tennis → 0 live candidates + provider_gap + insufficient_live_data warnings (honest); sports_baseball → 60 live candidates, quote mix 35 tight/25 missing (settled), volatility 35 calm/25 insufficient at pre-game time, mean freshness ~375s. OBSERVATION ONLY: no EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution/autonomy; changes no MarketOps/EDGE-AUTO/forecast/gate/flag behavior |
| COST-MODEL-001 | (this) | Read-only COST-ADJUSTED follow-through measurement — does any shadow cohort survive spread/fee/executable-price friction? New `edge-cost-shadow-report --hours N --top N` + `EdgeCostShadowReportService`: per watchlist row it re-measures the recorded 60m midpoint follow-through net of (a) half-spread (snapshot spread, tick fallback), (b) a documented conservative Kalshi fee assumption (new config `kalshi_fee_rate_assumption`=0.07 — the published taker shape rate·P·(1−P) charged at BOTH the trigger and horizon, full round trip, no rebates/rounding-down), and (c) executable TOUCH prices from recorded bid/ask ticks (forecast above market: trigger ask→horizon bid; below: trigger bid→horizon ask — the NO-side identity; missing quotes = uncovered, never guessed). Cohort breakdowns: baseline, all pre-registered EDGE-SELECTION policies, market types, gap_follows/opposes, liquidity/spread/confidence buckets, top series. Per-cohort: n, touch coverage, frictionless toward/closure, net-of-half-spread, fee-adjusted, executable-touch closures + conservative labels (`too_thin`/`cost_killed`/`neutral`/`promising_friction_adjusted_shadow` needing n≥75 + positive touch AND fee-adjusted + toward≥0.55 + concentration + out-of-sample window for prereg policies) + a cohorts-positive-after-costs line + unconditional MVP-005B-blocked note. **Live finding (48h, 322 measurable rows, 100% touch coverage): every positive-frictionless cohort is `cost_killed`** — all four follows-move cohorts (frictionless +0.10..+0.30) go NEGATIVE after the fee assumption (−0.03..−0.21); touch-only numbers are thin (+0.02..+0.11) and the fee erases them; `cohorts_positive_after_costs: NONE` on 48h (24h shows one small window-dependent survivor). The apparent shadow edge is, so far, a frictionless-measurement artifact — strengthening the case that EDGE-SELECTION validation must include cost-adjusted metrics before any MVP-005B discussion. MEASUREMENT ONLY: friction-adjusted closure is movement net of an assumption, not EV/PnL; changes no gate/trigger/forecast/promotion/flag/MarketOps/EDGE-AUTO behavior; persists nothing; no external call; no EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution |
| EDGE-SELECTION-001 | (this) | Pre-registered, read-only VALIDATION PROTOCOL for the row-selection candidates from EDGE-FILTER-001 (confirmed selection-not-timing by TRIGGER-TIMING-001). New `docs/EDGE_SELECTION_PREREG_2026_07_09.md` (locked 2026-07-09T19:00Z) freezes 8 policies — baseline_all_watchlist; candidates require_gap_follows_move_totals_only (primary), require_gap_follows_move_exclude_spreads, gap_follows_move_and_high_liquidity, gap_follows_move_and_tight_spread, total_only, exclude_spread_markets; `spread_only` as NEGATIVE CONTROL (expected to stay adverse; a non-adverse control flags the window as suspect) — plus fixed success gates (final_n≥75 hard/≥150 preferred; 60m toward≥0.55; positive mean closure; ≤34% single-ticker/≤50% single-game concentration; out-of-sample window; no MarketOps/safety regression; clean invalid profile) and failure gates (toward<0.50; negative closure; sample collapse <12; concentration; discovery-only pattern). New `edge-selection-validation-report --hours N [--since ISO] [--until ISO]` + `EdgeSelectionValidationReportService`: evaluates ONLY the frozen registry (tests enforce — no arbitrary policies), classifies every window discovery/validation/mixed vs the lock (discovery/mixed can NEVER validate; the report says so), reports per-policy n/toward/closure/concentration/market-type mix with per-gate PASS/fail and protocol statuses (validated_shadow / passes_gates_discovery_only / insufficient_sample / inconclusive_continue_observing / failing_gates / sample_collapsed / control_consistent / control_anomaly), and ALWAYS prints "MVP-005B remains blocked unless explicit human acceptance" + an explicit overfitting note. **Motivating evidence recorded in the prereg doc: require_gap_follows_move_totals_only cleared the shadow MVP bar on one 48h window (2026-07-09 ~17:45 UTC) and regressed below it within hours (24h toward 0.389)** — single-window winners of an 18-policy search are upward-biased by construction. Validation windows pre-declared: next 24h/48h post-lock, next MLB slate, WC Jul 9–11 (if post-lock rows), rolling 7d (~2026-07-16, primary decision window). Current-data run behaves correctly: window=DISCOVERY, no candidate validated, control consistent. PROTOCOL ONLY: changes no gate/trigger/forecast/promotion/flag/MarketOps/EDGE-AUTO behavior; persists nothing; no external call; not PnL; no EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution |
| TRIGGER-TIMING-001 | (this) | Read-only SHADOW timing simulation — the natural follow-up to FORECAST-ANCHOR-001's `timing_adverse_selection` verdict: would measuring LATER have helped? New `trigger-timing-shadow-report --hours N [--top N]` + `TriggerTimingShadowReportService`: for each historical watchlist row it replays 8 timing policies over the SAME persisted ticks (baseline_immediate; +2/5/10/15m cooldowns; wait_until_midpoint_flat_5m ±0.01 band; wait_until_spread_stable; wait_until_gap_follows_move — conditional waits bounded at 30m), holding the recorded forecast fixed, re-deriving the gap from the midpoint at the delayed time (delayed measurements use post-trigger ticks only), counting `gap_evaporated` when the delayed gap fell below the live min-abs-gap (mean reversion beat the measurement — evidence in itself), recomputing pre-move/opposes at the delayed time, and measuring 5/15/30/60m follow-through FROM the delayed time. Per-policy: measurable/lost/survival, mixes, opposes-share, horizons, paths, drift, concentration guards, and the shared conservative label ladder (promising_shadow same bars as EDGE-FILTER-001); plus improved/worsened-by-delay examples and a five-question comparison (opposes trend, closure trend, survival vs evaporation, market-type mix under delay, cooldown vs condition-wait). **Validation on live host data (48h, 256 rows): no timing policy is promising_shadow.** Delays soften closure (baseline −0.39 → best delay_5m −0.19 @60m) but toward-rate does NOT improve (0.263 → ~0.23); flat-wait keeps only 50% of rows and grades worse_than_baseline; wait_until_gap_follows_move drives opposes-share to 0 with 76% survival yet closure stays negative (−0.19) — **a pure cooldown is NOT the fix; condition-waits trade sample for modest softening**. Forecast held fixed is a stated caveat (live forecaster could refresh during a delay). SHADOW ONLY: changes no trigger/gate/threshold/forecast/promotion/flag/timer/MarketOps/EDGE-AUTO behavior; persists nothing; no external call; not PnL; no EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution |
| FORECAST-ANCHOR-001 | (this) | Read-only forecaster-ANCHORING diagnostic — did the forecast move when the market moved? New `forecast-anchor-diagnostic-report --hours N [--top N]`: per watchlist row it reconstructs midpoints at measurement and 5m/10m before, joins the PRIOR forecast for the ticker, computes forecast_delta and market_delta over the same interval, an adjustment ratio |Δf|/|Δm|, and a deterministic anchor bucket (anchored_static <0.01 Δf vs ≥0.02 Δm; partial_adjustment ratio<0.5; moved_with_market; moved_against_market; no_prior_forecast; insufficient_data), then aggregates 11 dimensions with verdicts (too_thin / insufficient_prior_forecast_data / anchoring_confirmed / timing_adverse_selection / market_type_specific overall override / no_anchor_issue_detected) + follow-through BY anchor bucket + examples + a six-question interpretation. **Live finding (48h, 262 rows, 125 classifiable — 49% had no prior forecast): the anchoring hypothesis is REFINED, not confirmed.** Only 7% of classifiable forecasts were fully static; median adjustment ratio 0.467 (the forecast moves ~half as much as the market; market moved more in 84% of rows) — but follow-through by bucket shows even `moved_with_market` rows fail (toward 0.19 / closure −0.31 vs partial 0.13/−0.67): keeping up does NOT rescue follow-through. Spreads and totals have near-identical anchored shares (0.445 vs 0.452) yet totals grade `no_anchor_issue_detected` (toward 0.386) while spreads grade `timing_adverse_selection` (0.192) — **totals are not less anchored, just less adverse; the dominant mechanism is trigger timing/selection, not the forecaster**. OVERALL VERDICT: `timing_adverse_selection`; next-step evidence line: trigger-redesign candidate OR more shadow data — any change remains a separate explicitly-accepted milestone. ANALYSIS ONLY: changes no forecast/gate/promotion/flag/MarketOps/EDGE-AUTO behavior; persists nothing; no external call; not PnL; no EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution |
| EDGE-FILTER-001 | (this) | Read-only SHADOW filter analysis over FOLLOWTHROUGH-001 row diagnostics — what would watchlist follow-through have looked like under 18 candidate adverse-selection filters (baseline, gap-opposes/follows-move, sharp-pre-move variants, signal-type/market-type/series exclusions incl. data-derived worst series, and combined follows-move+quality policies)? New `edge-filter-shadow-report --hours N --top N` + `EdgeFilterShadowReportService` (consumes FOLLOWTHROUGH-001's RowDiagnostic; per-policy survival ratio, market/signal/series/gap-sign mixes, per-horizon toward/closure, path rates, spread/liquidity drift, removed/retained examples, and a conservative label ladder shared with EDGE-POLICY-001: promising_shadow requires final_n>=30 + (toward>=0.55 @30/60m OR 60m closure>=+0.10) + no single-ticker(>34%)/single-game(>50%) concentration; bar-clearing-but-undersampled cohorts label too_thin/keep-observing — a live mislabel found and fixed: selective young cohorts are not `reject_policy`). **Live 48h shadow results (262 rows):** three promising_shadow policies — `gap_follows_move_and_tight_spread` (n=53, toward 0.396, closure +0.177), `gap_follows_move_and_high_liquidity` (n=30, 0.467/+0.225), `require_gap_follows_move_exclude_spreads` (n=38, 0.447/+0.160); strongest-but-young: `require_gap_follows_move_totals_only` (n=26, **toward 0.539/closure +0.42**) and `totals_only_no_sharp_pre_move` (n=15, 0.533/+1.19) → too_thin/keep observing. Interpretation from data: excluding gap-opposes improves 60m toward by +0.036 (helpful, insufficient alone); spreads confirmed the adverse-selection concentration (spread_only 0.192 vs exclude-spreads 0.327); totals materially less bad (+0.110 vs baseline); **no policy clears the MVP-005B bar (n>=20 AND toward>=0.55) → MVP-005B remains BLOCKED**. SHADOW ONLY: re-slices existing rows; changes no live gate/threshold/promotion/forecast/flag/MarketOps/EDGE-AUTO behavior; persists nothing; no external call; not PnL; no EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution |
| FOLLOWTHROUGH-001 | (this) | Read-only diagnostic for WHY edge-precheck gap follow-through is negative (frontier showed toward-rate 0.33@5m→0.24@60m, closure −0.17→−0.78, all sports_baseball). New `edge-followthrough-diagnostic-report --hours N [--top N]` + `EdgeFollowthroughDiagnosticService`: per-row TIMING (signal/forecast/snapshot ages incl. signal-age from the signal row; midpoint move in the 10 min BEFORE measurement; `gap_vs_pre_move` relation — a gap that points back where the market came from is a forecast lagging a move), DIRECTION (per-horizon continued_away/reverted_toward/flat on the same closure convention as frontier/cohort so numbers reconcile; 60m spread/liquidity drift), deterministic per-cohort VERDICTS (`too_thin`→`stale_or_chasing_move`→`adverse_selection_candidate`→`measurement_artifact_possible`→`promising_needs_more_sample`→`neutral`, documented priority + thresholds), 16 cohort dimensions (incl. series/ticker, forecaster, evidence, signal/forecast/snapshot age buckets, gap_vs_pre_move), and concrete failure examples (largest negative closure, repeated ticker failures, fresh-forecast-adverse, stale-snapshot rows). **Live finding (real host data, 114 rows):** overall verdict `adverse_selection_candidate` — sharp_pre_move_share 0.81, gap_opposes_move_share 0.80, forecast_age_p50 41s (forecasts are FRESH — not staleness: the evidence forecaster re-forecasts after the trigger but stays anchored behind the move); the discriminating cohort: gap-opposes-move rows toward 0.20/closure −0.93 vs gap-follows-move 0.38/−0.16; KXMLBSPREAD worst (0.13/−1.03, opposes 94%). ANALYSIS ONLY: changes no gate/forecast/promotion/EDGE-AUTO/MarketOps behavior; not PnL, no EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution; no migration/flag/timer/external call |
| OPS-013 | (this) | Production-safe tick aggregation (operational storage/durability only). Root-caused from the OPS-012 deploy: one ~49s full-window commit held the SQLite write lock and collided with a MarketOps cycle (#1215 PendingRollbackError). `TickAggregationService` now commits **per sub-window** (default 1h, `--subwindow-hours`) so lock hold is seconds, with per-window stats (rows/buckets/duration/commit_ms/retries) and **bounded retry on a locked DB** — implemented as an apply+commit UNIT that re-applies the upserts on every attempt, because a rollback discards pending work and a bare commit-retry would commit an empty transaction and falsely report success (caught by test, fixed). A window whose commit still fails is rolled back, recorded LOUDLY (stats + audit row + nonzero exit), and the pass continues — the idempotent rerun repairs it; an oversized window (runaway guard, `TICK_AGGREGATION_MAX_ROWS_PER_SUBWINDOW=100k`) is skipped loudly. New audit spine `tick_aggregation_runs` (migration 0024; pruned at watcher_run_days) records every pass — the EVIDENCE for readiness. Timer artifacts `infra/systemd/user/probability-arena-tick-aggregation.{timer,service}` (hourly, `--scheduled --hours 12`, overlap-by-design so cycles self-heal) exist but are **NOT auto-installed**, and `--scheduled` no-ops unless `ENABLE_TICK_AGGREGATION_TIMER=true` (default **false**; manual runs always allowed). `tick-aggregation-report` gains a **raw-retention-reduction READINESS section** (evidence only, enacts nothing): coverage_72h>=0.98, >=5 clean SCHEDULED cycles, no recent run errors, raw feed still fresh — all gates must pass before a FUTURE milestone may stage the 3d→24-48h reduction. **Raw tick retention is UNCHANGED**; no raw tick deletion; buckets remain telemetry summaries, never trading signals. No EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket/MEME-MAS unchanged |
| OPS-012 | (this) | Tick aggregation / DB pressure control (operational storage/durability only). `market_price_ticks` dominates SQLite growth on EVO-X2 (~62% of the file: 1,698 MiB / 613k rows incl. raw_payload ≈ 2.84 KB/row; ~8.4k ticks/h ≈ 558 MiB/day at a 3d window, DB 2,728 vs crit 3,072 MiB). New `market_price_tick_buckets` (migration 0023): fixed-interval OHLC-midpoint + open/close bid/ask + spread/liquidity min/max/avg + tick_count summaries, unique-keyed (ticker, bucket_start, bucket_seconds). `TickAggregationService`: epoch-aligned buckets (default 300s; must divide 3600), hour-aligned bounded sub-windows with a raw-row cap (truncation stops on an hour boundary and is REPORTED, never silent), idempotent upsert (rerun => identical values, no duplicates), missing midpoints => NULL OHLC (never fabricated), unusable rows counted+skipped. **Raw ticks are NEVER deleted by aggregation** and raw tick retention is UNCHANGED in OPS-012; buckets get their own 90d retention window (`TICK_BUCKET_RETENTION_DAYS`). New CLI `aggregate-market-ticks [--hours --bucket-seconds --dry-run --max-rows]` + `tick-aggregation-report` (coverage by hour, compression ratio, and a STAGED — not enacted — recommendation: only after coverage is proven healthy may a FUTURE milestone reduce raw retention toward 24-48h). `db-growth-report` gains heaviest-tickers, projected steady-state under current retention, bucket counts, and warn/crit status. Validated at live scale on a scratch copy of 24h of real host ticks: 202,950 raw -> 43,564 buckets in ~35s, coverage 25/25 hours (healthy), idempotent rerun inserted=0/updated=43,564, raw count unchanged; bucket bytes ~100x smaller than raw (fixed-width numerics vs ~2.8 KB payload rows). Buckets are TELEMETRY SUMMARIES, never trading signals — no EV, no paper trading, no recommendations, no sizing, no orders, no wallets/keys/signing/swaps/execution; watcher/MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket logic unchanged |
| XVENUE-OBS-001 | (this) | Read-only cross-venue observation-window RUNBOOK + report for high-overlap slates (World Cup semifinal/final, MLB slates, election windows). New `docs/XVENUE_OBSERVATION_RUNBOOK.md` (targeted scan → coverage census → match → report → candidates-by-label → window verdict, with measured domain/window guidance: WC game-winner↔game-winner is the realistic clean shape; MLB needs a tight `--end-date-max` to keep game markets over draft/season futures; politics is census-first — Kalshi supply thin + resolution gaps dominate; crypto has no comparable supply; tennis only match-winner↔match-winner counts). New `xvenue-observation-report [--top]` composes the latest persisted scan run + match run into one window view: scan provenance, rows considered, comparable **clean vs flagged** split (clean = no `large_observed_difference_requires_review`), side-uncertain/unresolved counts, mismatch reasons, sample clean candidates, and an overlap assessment (`no_scan_data` / `no_match_run` / `insufficient_overlap` / `overlap_no_clean_comparable` / `clean_comparable_present`) — an observation-COVERAGE verdict for human review, never a signal; also warns when the match run predates the scan. Derived on demand from persisted rows: **no external call, no persistence, no migration, no flag, no timer, no new match label**. Validated against the real host rows (389 candidates → `overlap_no_clean_comparable`, the flagged ATP set-winner correctly excluded from clean). No EV/arbitrage/opportunity/trade language; no sizing/orders/wallets/keys/signing/swaps/execution/autonomy; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket-scan unchanged |
| XVENUE-OPS-001 | (this) | Read-only USABILITY/coverage fix for manual cross-venue matching. `cross-venue-match-once` loaded active Kalshi markets in rowid order (oldest-inserted first) — on the long-running host those are days-stale rows still flagged `active` (July-2 games, `last_seen` 5-6d old) with no overlap against the freshly-scanned Polymarket sample, so the no-arg default produced 0 candidates. Now loads **most-recently-seen first** (`last_seen_at DESC, id DESC`) with tiered fallback (recency-window → active → any-status), new options `--recent-hours` (drops markets not seen in the window; live: `--recent-hours 48` skipped 11,935 stale `active` rows), `--domain`, `--market-type`, and raised bounded defaults (kalshi 1500→**4000**, polymarket 200→**500**) so the no-arg run is representative without magic limits. Every run prints a transient `MatchSampleComposition` (rows loaded/considered, per-domain + per-market-type breakdown both venues, stale/no-snapshot counts, domain overlap, low-overlap coverage note) — diagnostics only, not persisted, no migration. Live A/B on a scratch DB seeded with real host rows: **no-arg default 0 → 389 candidates** (old loaded stale July-2 games, new loads fresh July-9), matcher/labels/gates byte-for-byte unchanged. **Changes only WHICH persisted rows are considered — no EV, no arbitrage/opportunity language, no forced matches, no sizing/orders/wallets/keys/signing/swaps/execution/autonomy**; no flag, no timer, no endpoint, no external call; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket-scan unchanged |
| POLY-PRECISION-001 | (this) | Cross-venue matcher PRECISION hardening, required before POLY-COVERAGE-001 may deploy. Fixes two POLY-002 defects surfaced by the coverage expansion: (1) the Polymarket midpoint was read from `outcome_prices[0]` regardless of which proposition that outcome names — the market book prices `outcomes[0]`, verified as a TEAM NAME in ~26% of live markets (`["Kansas City Royals","New York Mets"]`), while Kalshi encodes a game market's Yes side in the TICKER SUFFIX, not the title (`...SDLAD-SD` and `...-LAD` share "San Diego vs Los Angeles D Winner?"); (2) "O/U 2.5" normalized to `yes_no` because `normalize_title` stripped the slash before the over/under test, and the `[+-]\d` handicap test could never fire for the same reason. New `resolve_poly_yes_index` / `poly_yes_midpoint` produce a midpoint ONLY when the Polymarket outcome is aligned to the Kalshi YES side (Yes/No labels incl. reversed ordering, Over/Under direction, or a uniquely-matched named entity); otherwise midpoint AND `observed_difference` are ABSENT with `outcome_side_uncertain`/`midpoint_side_uncertain` and the pair stays `unresolved_semantic_match`. Adds a tightened outcome-type compatibility map (yes_no no longer matches winner), `market_scope` (player_prop / game / tournament_future / event wildcard), threshold + handicap-line parsing, a new `exact_score` type with ENTITY-ANCHORED scoreline comparison ("Spain wins 3-1" vs "Spain 1 - 3 Belgium"), named-entity overlap with generic-word filtering, prefix-anchored sport identity, and a `large_observed_difference_requires_review` REVIEW flag (>0.35 gap, confidence <0.85; never rejects, never an opportunity). `_best_match` now prefers a plausible COMPATIBLE partner while still recording the rejection when none exists. Live-validated on a scratch DB seeded with real Kalshi rows against the SAME 396-market Polymarket sample: candidates 143→143 (audit rows preserved), `comparable_market_candidate` **9→2**, measured-gap p50 **0.39→0.125**; the 5 KXMLBGAME false pairs (gaps 0.37–0.49) became `outcome_side_uncertain`, and the 2 survivors are the genuinely identical GPT-5.6 markets, both correctly flagged for review (stale Kalshi quote). Read-only measurement only: **no arbitrage, no EV, no trade recommendation, no paper trading, no sizing, no orders, no wallets/keys/signing/swaps/execution**; no flag, no timer, no migration, no external call; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket-scan behavior unchanged |
| POLY-COVERAGE-001 | (this) | Read-only Polymarket COVERAGE expansion so POLY-002 has comparable supply (POLY-002 measured 0 `comparable_market_candidate` against a 50-market, tournament-winner-heavy sample — the constraint was SUPPLY, not the matcher). Adapter gains bounded catalog pagination (Gamma `offset`; server caps a page at 100), `tag_id` category + `end_date_min`/`end_date_max` resolution-window filters, and a bounded `/public-search` walk (paginates by `page`; **`offset` is silently ignored** — nested search markets omit `endDate`/`events`, both inherited from the parent event, without which a search-sourced market could never be labelled comparable). Deterministic Kalshi-derived targeting: a fixed topic registry evidenced by persisted Kalshi ACTIVE whole-word title terms **and prefix-anchored ticker series** (`KXWC*`/`KXMLB*`/`KXITF*` — Kalshi's `category` is empty and its titles are game-prop text, so ~1100 World Cup + ~1160 tennis markets are invisible to title-only matching; a `FED` substring matches pitcher Erick Fedde's MLB ticker, so prefixes are anchored). **No LLM, no external taxonomy, no new/paid provider.** Targeted queries claim the budget first, each capped at a fair share of the remaining budget so one high-yield topic cannot starve the others; skips/caps/truncation are logged, never silent. `polymarket-scan-once` gains `--limit/--orderbook-limit/--category/--active-only/--no-active-only/--include-closed/--query/--targeted/--end-date-min/--end-date-max`; new `polymarket-coverage-report [--top --kalshi-limit]` is a per-domain/per-market-type SUPPLY census (both venues, order-book coverage, two-sided rate, spread/depth, overlap, and the REASON a domain lacks comparable supply). Scan provenance persisted on `polymarket_scout_runs` (`scan_mode`, `pages_fetched`, `market_fetch_errors`, `duplicates_dropped`, `queries_used` = queries actually SENT; migration 0022). Live-validated on a scratch DB seeded with real Kalshi rows: 50→396 Polymarket markets, 1→4 domains, cross-venue candidates 20→141 and `comparable_market_candidate` **0→8** — though the resulting `observed_difference` distribution (p50 0.49) shows most of those 8 are matcher false positives, so the binding constraint MOVED from coverage to matcher precision — addressed by POLY-PRECISION-001, which must ship before this deploys. **Coverage expansion only: identifies no arbitrage, computes no EV, recommends no trades, paper trades nothing, sizes nothing, places no orders, uses no wallets/keys/signing/swaps/execution, and forces no match.** `comparable_supply` = a comparison could be ATTEMPTED, never an opportunity. `ENABLE_POLYMARKET_SCOUT` remains false; no timer; public/no-auth GETs only; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/POLY-002-matcher behavior unchanged |
| POLY-002 | (this) | Read-only Kalshi↔Polymarket cross-venue OBSERVATION (realizes the POLY-001 placeholder). Deterministic semantic normalizer (title/outcome/date) + `CrossVenueMatchingService` over already-persisted Kalshi markets/snapshots + POLY-001 polymarket markets → candidates labelled `comparable_market_candidate` / `unresolved_semantic_match` / `incompatible_outcome` / `incompatible_resolution` / `low_confidence_match`, with MEASURED observation fields (midpoints on a 0–1 probability scale, spreads, liquidity proxies, `observed_difference` = |kalshi_mid − polymarket_mid|). 2 tables (migration 0021), `cross-venue-match-once` / `cross-venue-report` / `cross-venue-candidates`. **Identifies comparable markets and measures differences — it does NOT compute EV, label arbitrage/"arb", recommend trades, paper trade, size, place orders, or use wallets/keys/signing/swaps/execution** (no side/size/EV/action/order/wallet field exists by construction). No external call (matches persisted rows); no timer; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket-scan unchanged |
| MEME-MAS-003 | (this) | Multi-objective calibration METRICS for MEME-MAS review_priority (no label change): MEME-SHADOW gains five separate objective sections — `momentum_followthrough` (positive-move rate + median price per horizon), `survival_quality` (survival/rug/severe-end), `risk_adjusted_movement` (median move × survival — a measured diagnostic, never a return/PnL), `review_queue_efficiency` (queue share + momentum-positive lift vs overall), and `coverage_quality` (label-independent covered-vs-missing outcomes). `meme-mas-objectives-report [--lookback-hours]` prints them v1 vs v2 — showing high_review is momentum-positive even when survival-lower, elevated_review is safer, and missing coverage/concentration predict worse outcomes. Analysis only: scoring/labels UNCHANGED; compute-on-demand, no table/migration/external call/budget impact; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket unchanged. No EV/trade/paper/sizing/orders/wallets/keys/signing/swaps/execution |
| MEME-MAS-002 | (this) | Recalibration of the MEME-MAS diagnostic labels (informed by MEME-SHADOW-001: missing coverage + concentration predicted worse survival, but the velocity-heavy composite diluted them). Profile-based scorer (`v2` default, `v1` preserved for comparison): heavier risk penalties (missing coverage 0.3→0.55, concentration 0.6→0.7, fake-volume/liquidity-removed 0.5→0.6), full risk dampening `(1−risk)`, momentum(0.55)+structure(0.45) composite, and **gated high_review** (requires strong momentum + clean structure + non-missing provider coverage + no concentration flags). New first-class quality outputs `momentum_quality` / `structure_quality` / `coverage_quality`; `reject_risk` hard gates preserved. `meme-mas-calibration-report` runs MEME-SHADOW under v1 vs v2 for a before/after (high_review share + per-priority survival). Read-only label calibration — no EV/trade/paper/sizing/orders/wallets/keys/signing/swaps/execution; compute-on-demand; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket unchanged |
| MEME-SHADOW-001 | (this) | Read-only follow-through / calibration analysis of MEME-MAS `review_priority` labels: reconstructs the label at each historical attention snapshot (reusing the MEME-MAS agents, risk assessment as-of that moment) and measures the SAME token's later trajectory — price/liquidity/volume change at 5m/15m/1h/6h/24h, survival, rug/liquidity-removed incidence, attention persistence, risk-level transition. `meme-shadow-report [--lookback-hours]` aggregates outcomes by review_priority / review-score bucket / risk-penalty bucket / risk reason / concentration bucket, with conservative calibration labels (`too_thin`/`measured`) + a recommendation (`too_thin_to_calibrate`/`labels_separate_outcomes`/`no_material_separation_recalibrate`/`review_priority_inverted_recheck`). Market-movement MEASUREMENT (like edge follow-through) — NOT PnL/EV/paper/recommendation/sizing/orders; compute-on-demand (no table/migration/external call/budget impact); changes no label, authorizes nothing; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket unchanged |
| MEME-MAS-001 | (this) | Read-only multi-agent memecoin DIAGNOSTIC scoring layer: 5 deterministic agents (Coin Structure, Catalyst Velocity, Timing, Risk Auditor, Composite Review — pure functions, no LLM, no external calls, no new providers) recompute sub-scores ON DEMAND from persisted `meme_attention_snapshots` + `crypto_token_risk_assessments` + `meme_catalyst_events` → a `review_priority` (low/monitor/elevated_review/high_review/reject_risk) that triages human-review attention. `meme-mas-report [--hours --top]` (top candidates, risk rejects, missing coverage, sub-score distributions, reason traces, provider coverage) + `meme-mas-assess [--limit]` (per-token traces). No table/migration (compute-on-demand; all inputs already persisted), no external request, no provider-budget impact. `review_priority` is NOT a trade recommendation/EV/sizing/order/buy/sell/bet — `reject_risk` is avoid/flag for review; no wallets/keys/swaps/signing/execution; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket behavior unchanged |
| SOLANA-TRACKER-002 | (this) | Sniper/insider/bundler coverage from the SolanaTracker `/tokens/{address}` risk object. The data was already fetched (SAME endpoint, no extra request, no budget impact) but mis-parsed: the concentration fields arrive as `{count, totalBalance, totalPercentage, wallets}` and the field is `totalPercentage` (0-100 percent), not the old `percentage`. Fix reads `totalPercentage` directly (new `_percent_direct`, no ratio mis-scaling of sub-1% values), keeps the legacy `percentage` ratio fallback + bare-number `top10`, and stays absent (no fabrication) on missing keys. Live-validated: coverage 0% → sniper 100% / insider 100% / bundler ~83% (real values e.g. 19.1%/12.8%/7.5%). Activates the dormant `sniper_concentration`/`insider_concentration`/`bundler_concentration` heuristic categories (thresholds 20/15/25) on real data. Read-only risk intelligence — no EV/trade/sizing/orders/wallets/keys/signing/swaps/execution; GoPlus/Birdeye/budget guardrails/MarketOps/EDGE-AUTO/MEME-NEWS/Polymarket unchanged; no migration |
| PROVIDER-BUDGET-001 | (this) | SolanaTracker request accounting + budget guardrails (cost/usage observability). Usage derived read-only from `crypto_token_risk_assessments` (no new table); `crypto-provider-budget-report` (plan/limit, requests today/hour/month, estimated monthly run-rate, remaining daily/monthly budget, success/error rate, coverage-per-request, keep/tune rec). 7 budget knobs (`SOLANA_TRACKER_MONTHLY_REQUEST_LIMIT=200000`, `_DAILY_REQUEST_BUDGET=5000`, `_HOURLY_REQUEST_BUDGET=200`, `_PER_RUN_LOOKUP_LIMIT=25`, `_CACHE_TTL_HOURS=24`, `_WARN_DAILY_REQUESTS=4000`, `_STOP_DAILY_REQUESTS=6000`). Guardrail only SKIPS optional SolanaTracker lookups over budget (per-run cap / daily STOP → fallback GoPlus+heuristics); never adds calls, never touches GoPlus/Birdeye. SolanaTracker Advanced ≈ $58–59/mo OpEx documented (accounting metadata). No EV/paper/recommendation/sizing/orders/wallets/keys/signing/swaps/execution; MarketOps/EDGE-AUTO/MEME-NEWS/Polymarket behavior unchanged |
| POLY-001 | (this) | Read-only Polymarket market-data observer (SECOND prediction-market venue). Public/no-auth `PolymarketAdapter` (Gamma market catalog + CLOB read-only order books) → `PolymarketScoutService.scan_once` persists `polymarket_markets` / `polymarket_orderbook_snapshots` / `polymarket_domain_inventory_snapshots` + an audit `polymarket_scout_runs` (migration 0020); `polymarket-scan-once [--scheduled]`, `polymarket-report` (markets seen/active/categories, two-sided + orderbook availability, spread/depth/liquidity proxies, newest + highest-volume/liquidity markets, provider health, row counts), `polymarket-domain-report` (per-category inventory). `ENABLE_POLYMARKET_SCOUT` (default false) reserves loop/timer use only (manual always allowed; no timer installed); `POLYMARKET_RETENTION_DAYS=14`. Market-DATA observation only — prices/order books are informational quotes, never EV/recommendation/instruction/trade trigger; authenticated CLOB trading endpoints deliberately NOT implemented; no API keys/wallets/keys/signing/swaps/orders/sizing/execution; cross-venue Kalshi linking shipped in POLY-002; coverage broadened by POLY-COVERAGE-001 (no arb/EV/trade-candidate labels); Kalshi/MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker behavior unchanged |

## Immediate next steps

1. Roll out SCANNER-002/OPS-010 on EVO-X2 and validate during the next live game-level window (POR–ESP Jul 6 / ARG–EGY Jul 7 / MLB evenings): game-level markets in the scan, watcher ticking them, signals → `soccer_evidence`/`baseball_evidence` forecasts at ≥0.60 confidence → first valid watchlist rows.
2. Run targeted `edge-precheck --latest-marketops-run` sessions during those windows; on sane watchlist behavior, consider `MARKETOPS_INCLUDE_EDGE_PRECHECK=true`.
3. Keep accumulating champion/challenger pairs toward `useful_sample` (n≥100) for both `baseball_evidence_v1` and (as data arrives) `soccer_evidence_v1` cohorts.
4. Consider the remaining CAN–MAR promotion-tuning ideas (harder player-prop deprioritization; KXWCAST/KXWCSOA classification) once game-level supply exists to compare against.

## Gated future steps (in order; each requires explicit acceptance)

- **MVP-005B — paper simulator**: gated on accumulated edge-precheck measurement data (watchlist/paper_candidate_later precision over time) + its own explicit acceptance. Simulation only; still no orders.
- **CRYPTO-003 — crypto paper simulator**: gated like MVP-005B; simulation only, no orders, no wallets; requires CRYPTO-002 risk data to mature first.
- **OPS-012 — tick aggregation (proposed)**: `market_price_ticks` is the dominant storage growth source. Future milestone: roll raw ticks into hourly OHLC/spread/liquidity aggregates, retain raw ticks shorter (e.g. 3d) and aggregates longer, and move DB-growth alerting from absolute-size gates to a rate-based (MiB/day over a window) signal. Read-only/ops; no alpha change. Build only when small and explicitly safe.
- **POLY-003 — Polymarket live WebSocket (read-only)**: the cross-venue semantic linker shipped in **POLY-002** (see the milestone table). What remains optional/future is a live CLOB market-channel WebSocket (subscriptions only, bounded, dark by default) for lower-latency order-book observation. Strictly read-only — **no arbitrage, no EV, no trade-candidate labels, no execution**; build only when small and explicitly safe.
- **WALLET-001 — policy-controlled transaction proposal gateway**: *much later*; proposals only — no signing, no private keys, behind a dedicated custody/security review — see `docs/SAFETY_BOUNDARIES.md` and ADR-002.
