# CAPABILITY_MATRIX

| Capability | Current status | Allowed? | Required milestone before enabling/extending | Safety notes |
|---|---|---|---|---|
| Market scanning | Live (baseline timer, EVO-X2); generic first-N + targeted supported game-level series behind `ENABLE_TARGETED_MARKET_SCANS` (SCANNER-002/OPS-010) | ✅ | — | Read-only Kalshi GETs; MVE filter server-side; rate-bounded by limits + bounded 429 retries; coverage only — no EV/advice/trading surface |
| Signal detection (watcher) | Live (60s loop, EVO-X2) | ✅ | — | Informational signals only; cooldown dedup; retention bounds tick growth |
| External research | Canaries only: baseball via MLB Stats API behind `ENABLE_BASEBALL_EXTERNAL_RESEARCH`; soccer via provider-gated ESPN API behind `ENABLE_SOCCER_EXTERNAL_RESEARCH` + `SOCCER_RESEARCH_PROVIDER`; tennis (TENNIS-001) via provider-gated ESPN tennis API behind `ENABLE_TENNIS_EXTERNAL_RESEARCH` + `TENNIS_RESEARCH_PROVIDER` (match-winner only; `espn` payload mapping pending validation) — all default off/template. General `ENABLE_EXTERNAL_RESEARCH` (LLM+web) exists but off | ✅ behind flags | Per-domain canary review before widening further | Official sources preferred; honest template fallback; provenance persisted |
| Forecasting | Template baseline default; baseball + soccer evidence canaries behind their flags; tennis evidence canary (TENNIS-001, match-winner only, tightly-capped ±0.20 shift, conf cap 0.65) behind `ENABLE_TENNIS_EVIDENCE_FORECASTING`; LLM forecaster off | ✅ behind flags | Champion/challenger (MVP-004G) before broader rollout | Central confidence caps; capped prior shift; missing-facts→0.50/high-risk; deterministic fallbacks; measurement inputs only (no EV/trade/sizing) |
| Calibration | Live (outcome sync + scoring in baseline runner) | ✅ | — | Read-only scoring; append-only audit; the gate for everything below |
| MarketOps coordination | Autopilot (OPS-006): auto-promote/process signals, crypto scan, sync/score, champion/challenger snapshot, local DB alerts; loop/timer behind `ENABLE_MARKETOPS_AUTOPILOT` (default false), `marketops-run-once` manual | ✅ behind flag | — | Sequences existing read-only services only; cannot trade, paper trade, calculate EV, or move money |
| Frontier evaluation | EVAL-001: desk-wide quality report (signal/forecast/edge/follow-through/microstructure/crypto/latency/safety) + conservative readiness scorecard; `frontier-eval-report` CLI + `GET /eval/frontier-report` | ✅ | — | Evaluation only: gap follow-through is market-movement analysis (not PnL); readiness labels gate MEASUREMENT milestones and never authorize live capital |
| Edge precheck | MVP-005A: probability-gap **measurement** (forecast − midpoint) with 10 validity statuses, persistence counting, audit rows, CLI/API reports; behind `ENABLE_EDGE_PRECHECK` (default false), MarketOps stage double-gated | ✅ behind flags | MVP-005B (paper sim) gated on measurement-data maturity + explicit acceptance | No dollar EV/side/size/action fields exist by construction; `paper_candidate_later` is a review label with zero behavior |
| Edge cohort analysis | EDGE-ANALYSIS-001: `edge-cohort-report --hours N` — read-only cohort slicing (10 dimensions) of watchlist/candidate snapshots with per-cohort gap follow-through and conservative `too_thin`/`promising`/`neutral`/`weak`/`exclude_candidate` labels + MVP-005B gate readout | ✅ | — | Analysis/reporting only; follow-through is market-movement (not PnL); labels authorize nothing and change no flag/threshold/promotion/forecast/edge logic |
| **EV calculation** | **Does not exist** | ❌ | MVP-005A (design + safety review), itself gated on calibration evidence of edge | See SAFETY_BOUNDARIES; no placeholder surface allowed |
| **Paper trading** | **Does not exist** | ❌ | MVP-005B, gated on MVP-005A acceptance | Simulation only, still no orders; explicit human acceptance |
| **Live trading** | **Does not exist** | ❌ | Dedicated milestone, not on roadmap | Requires limits/kill-switch design first |
| **Wallet execution** | **Does not exist** | ❌ | Dedicated custody + security review milestone | ADR-002: no private keys in this repo/DB ever |
| Crypto scouting | Read-only Solana surveillance (CRYPTO-001): DEX Screener discovery, ticks, deterministic risk signals, reports; loop/timer use behind `ENABLE_CRYPTO_SCOUT` (default false), mock risk provider behind `ENABLE_CRYPTO_RISK_PROVIDER` | ✅ behind flags | CRYPTO-003 (paper sim) gated like MVP-005B | Public read-only GETs only; no wallets/keys/swaps/Jupiter/tx construction/signing — see SAFETY_BOUNDARIES |
| Crypto risk engine | CRYPTO-002: heuristics (always available) + optional GoPlus/SolanaTracker adapters behind `ENABLE_CRYPTO_RISK_ENGINE`/`ENABLE_GOPLUS_RISK`/`ENABLE_SOLANA_TRACKER_RISK` (all default false); composite scores/levels, activated risk signals, risk reports. SOLANA-TRACKER-002: sniper/insider/bundler now parsed from the SolanaTracker risk object's `totalPercentage` (same endpoint/request; coverage 0% → ~83–100% live), activating the sniper/insider/bundler heuristic categories | ✅ behind flags | CRYPTO-003 consumes this data after acceptance | Risk intelligence only — a score is an avoid/flag verdict, never trade advice; API keys header-only, never printed; no fabricated fields (missing → absent) |
| Holder-risk coverage | MEME-RISK-003: new Birdeye provider (holder + creator/deployer concentration) behind `ENABLE_BIRDEYE_RISK`; `creator_concentration` heuristic category; explicit provider coverage/health reporting (`crypto-provider-health-report`, `meme-risk-coverage-report`, holder-coverage overlay on `crypto-risk-report`) | ✅ behind flags | — | Read-only risk intelligence; provider absence stated explicitly (never silent); Birdeye payload mapping pending live validation → graceful absence; keys present/absent only; no EV/trade/sizing/orders/wallets/execution |
| Crypto lifecycle tape | CRYPTO-TAPE-001: read-only replayable token lifecycle recording — birth events, consolidated lifecycle snapshots (market + holder concentration + risk + social/catalyst + quote quality), actor observations (public-chain addresses only), and deterministic survival outcomes (survived_15m/1h/6h/24h, liquidity_removed, dead_volume, severe_risk, graduated_or_migrated, provider_gap); DERIVED from already-persisted rows (zero external calls, zero budget impact); `crypto-tape-run-once [--dry-run]` / `crypto-tape-report`; migration 0026 (5 tables, not retention-pruned) | ✅ (manual, read-only) | Scheduled tape assembly would need its own milestone/flag | Research infrastructure only — a survival label is measured token behavior, never PnL/EV/return/recommendation/sizing; no side/size/dollar/order/wallet column exists by construction; missing data named in missing_info, never fabricated; dry-run persists nothing; real runs persist ONLY tape rows; MarketOps unchanged |
| Crypto tape session helper | CRYPTO-TAPE-CADENCE-001: `crypto-tape-session --duration-hours N --interval-min N --limit N [--dry-run]` — fixed, hard-capped number of derived zero-external-call tape passes in ONE invocation (duration ≤36h, interval 15–120 min, ≤144 captures), abort on abnormal status/MarketOps error; session summary with horizon maturity + provider-gap trend | ✅ (manual, read-only) | A scheduled tape lane would need its own milestone/flag | Collection ergonomics only — not a timer/daemon, never autonomous; dry-run persists nothing and never sleeps; writes only ordinary tape rows; no EV/recommendations/sizing/orders/wallets/execution |
| Crypto horizon observation | CRYPTO-HORIZON-OBS-001/002: bounded market/liquidity observation lane filling the upstream tick-coverage gap — freeze a fixed cohort (`crypto-horizon-cohort-create`, ≤100), fetch due 15m/1h/6h/24h horizons via DexScreener (`crypto-horizon-observe-once`, ≤100 calls/pass, one fetch/token), persist ordinary ticks + audit rows so tape horizons mature; report completion/liquidity/gates (`crypto-horizon-observation-report [--shadow]`); migration 0027 (3 additive tables). **OBS-002**: deterministic `active_pair_quality_score` pair selection over all candidates (eligible = valid price + positive liquidity; activity/base-match preferred; stale penalised; honest no_liquidity_state, never fabricated from FDV/mcap/volume), retry-failed-in-place (no duplicate, observed frozen), `crypto-horizon-pair-selection-report` (avoidability + shadow policies) + `crypto-horizon-outcome-reconciliation-report` (with-vs-without-tick proof of unknown→known), reconciled explicit-denominator report; no migration | ✅ (manual or explicitly armed one-shot, read-only) | — | Observation only — misses recorded honestly (never fabricated); pair-quality selection is for OBSERVATION coverage, never a trade signal/EV/recommendation; DexScreener only → ZERO SolanaTracker impact; dry-run no-calls-no-persistence; success gates are measurement only; MarketOps unchanged; no EV/sizing/orders/wallets/keys/swaps/signing/execution |
| Crypto horizon manual scheduling | CRYPTO-HORIZON-SCHEDULE-001: `crypto-horizon-schedule-report` + `crypto-horizon-reminder-plan`; exact planner-derived UTC/PT windows, urgency/warnings, next-action command, and common-intersection reminder deduplication for frozen cohorts | ✅ (manual, report-only) | — | Compute-on-demand from persisted cohort state; no migration/write/artifact, provider call, timer/cron/daemon, flag, MarketOps/EDGE-AUTO hook, automatic observe invocation, or SolanaTracker impact. Suggested real commands require explicit human invocation; no EV/recommendation/sizing/order/wallet/signing/swap/execution/autonomy |
| Crypto horizon one-shot orchestration | CRYPTO-HORIZON-ORCHESTRATOR-001: `crypto-horizon-arm-cohort --dry-run/--confirm`, internal one-attempt worker, `crypto-horizon-orchestrator-report`, and isolated `crypto-horizon-disarm-cohort`; common-intersection groups become exact persistent one-shot `systemd --user` timers for an existing frozen cohort | ✅ (explicit confirm, read-only collection) | — | No migration/flag/automatic cohort creation/repeating timer/daemon/loop/sudo/secret output. Planner recheck prevents early/overdue/backfill calls; cohort-size call cap; one DB-lock retry; provider/MarketOps failures stop; post-success reports + self-cleanup. Host file audit only. DexScreener only, zero SolanaTracker; no MarketOps behavior or EV/recommendation/sizing/order/wallet/key/signing/swap/execution capability |
| Crypto tape coverage forensics | CRYPTO-COVERAGE-001: compute-on-demand diagnostic decomposing every unmeasurable survival horizon into an explicit cause (horizon_not_due / token_not_revisited_after_due / no_price_tick_near_horizon / no_pair_or_liquidity_state_near_horizon / no_risk_snapshot_near_horizon / outside_tolerance_only / source_rows_exist_but_join_failed / token_inactive_or_disappeared / unknown_gap_cause); per-horizon coverage funnel + gap histogram + upstream-vs-revisit bottleneck verdict + selection/starvation analysis + SHADOW-ONLY selection comparison (recent/due-first/fixed-cohort/mixed); `crypto-tape-coverage-report --hours N --top N --limit N`; no table/migration/flag/timer | ✅ (manual, read-only) | A live selection-policy change would be its own accepted milestone | Diagnostic only — coverage/timing measurement + shadow estimate; changes NO stored outcome label and NO live recorder selection; persists nothing, zero external calls, zero budget impact; MarketOps unchanged; never advice/EV/sizing/orders/wallets/execution |
| Crypto retrospective analysis | CRYPTO-RETROSPECT-001/002: compute-on-demand feature/outcome separation measurement over the lifecycle tape — concentration/risk/liquidity/volume/boost/attention/social/venue/coverage/missing-info cohorts vs survived_*/liquidity_removed/dead_volume/severe_risk/graduated/provider_gap; conservative ladder too_thin → provider_gap_dominates → no/weak/strong separators. **RETROSPECT-002** adds `--cohort {all,tape-backed,derived-only}`, a `data_source_mix` (per-source counts/maturity/gap-rate), and per-dimension `source_stratification` (all vs tape-backed vs derived-only + dilution warning + source labels tape_too_thin/tape_readable/all_window_diluted/derived_only_dominates/consistent_across_sources/tape_only_hint) so matured-tape signal is read apart from fresh-token noise; `crypto-retrospect-report --hours N --top N [--cohort …]`; no table/migration/flag/timer | ✅ (manual, read-only) | — | Measurement only — separation/source labels are feature-quality evidence for review triage, never advice/EV/sizing; unknown outcomes never enter rates; persists nothing, zero external calls, zero budget impact; MarketOps unchanged |
| Memecoin multi-objective calibration | MEME-MAS-003: read-only MEASUREMENT metrics extending MEME-SHADOW — `momentum_followthrough` / `survival_quality` / `risk_adjusted_movement` / `review_queue_efficiency` / `coverage_quality` per review_priority, v1 vs v2 via `meme-mas-objectives-report`; NO label change (scoring untouched); compute-on-demand | ✅ (manual, read-only) | — | Market-movement MEASUREMENT split by objective; `risk_adjusted_movement` = median move × survival (a diagnostic, never a return/PnL/EV); changes no label, authorizes nothing; no wallets/keys/swaps/signing/execution; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket unchanged |
| Memecoin label follow-through | MEME-SHADOW-001: read-only calibration MEASUREMENT — reconstructs MEME-MAS `review_priority` at each historical attention snapshot (as-of risk assessment) and measures the token's later trajectory (price/liq/vol change at 5m/15m/1h/6h/24h, survival, rug/liq-removed incidence, attention persistence, risk transition); aggregates outcomes by review_priority / sub-score bucket / risk reason / concentration bucket + a conservative calibration recommendation; `meme-shadow-report`; compute-on-demand (no table/migration/external call) | ✅ (manual, read-only) | — | Market-movement MEASUREMENT (like edge follow-through), NOT PnL/EV/paper/recommendation/sizing/orders; changes no label, authorizes nothing; no wallets/keys/swaps/signing/execution; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket unchanged |
| Memecoin diagnostic scoring | MEME-MAS-001/002: read-only multi-agent DIAGNOSTIC layer — deterministic agents (Coin Structure / Catalyst Velocity / Timing / Coverage / Risk Auditor / Composite Review; no LLM, no external calls) recompute sub-scores on demand from persisted meme/risk rows → `review_priority` + `momentum_quality`/`structure_quality`/`coverage_quality`; `meme-mas-report` / `meme-mas-assess`. **MEME-MAS-002** recalibration (profile v2 default; v1 preserved): risk-aware, heavier penalties for missing coverage + concentration, and **gated high_review** (clean structure + non-missing coverage + no concentration flags); `meme-mas-calibration-report` gives a v1→v2 before/after via MEME-SHADOW; no table, no migration, no provider-budget impact | ✅ (manual, read-only) | — | Diagnostic review-attention triage only — `review_priority` is NOT a trade recommendation/EV/sizing/order/buy/sell/bet; `reject_risk` is avoid/flag for review; no wallets/keys/swaps/signing/execution; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket unchanged; persists nothing |
| Provider request budget | PROVIDER-BUDGET-001: SolanaTracker request accounting + budget guardrails; `crypto-provider-budget-report` (plan/limit, requests today/hour/month, run-rate, remaining budget, success/error rate, coverage-per-request, keep/tune rec); per-run lookup cap + daily WARN/STOP thresholds; usage derived read-only from existing assessments (no new table) | ✅ | — | Cost/usage OBSERVABILITY only; guardrail can only SKIP optional SolanaTracker lookups over budget (fallback: GoPlus+heuristics), never adds calls, never touches GoPlus/Birdeye; SolanaTracker Advanced ≈ $58–59/mo OpEx (accounting metadata); no EV/trade/sizing/orders/wallets/keys/signing/swaps/execution |
| **Crypto wallet** | **Does not exist** | ❌ | Explicitly deferred ("wallet milestones later only") | Same as wallet execution |
| Meme/news scout | MEME-NEWS-001: read-only DexScreener token-genesis + boost-velocity surveillance → per-token `attention_score` + generic catalyst-event stream; `meme-scan-once`/`meme-scout-report`/`catalyst-report`; flags `ENABLE_MEME_SCOUT` (default false, manual always allowed) | ✅ | — | Discovery/scoring only; `attention_score` is an interest signal, never a buy/trade/EV/alpha score; no orders/wallets/keys/swaps/signing/execution; non-dexscreener catalyst sources are unconfigured placeholders |
| Domain-expansion scout | MEME-NEWS-001: read-only probability-market inventory grouped by domain/series with coverage proxies + `canary_priority` for candidate domains (weather/tennis/basketball/golf/esports/…); `domain-scout-report`; flag `ENABLE_DOMAIN_SCOUT` (default false, manual always allowed) | ✅ | — | Inventory/prioritization intelligence only; adds no forecaster, changes no promotion/edge/forecast logic; never advice |
| Scheduled meme/news lane | MEME-NEWS-002: bounded always-on read-only runner (`MemeNewsScoutRunner`) + windowed report + local derived alerts; `meme-news-run-once [--scheduled]`/`meme-news-report`/`meme-news-alerts`; systemd timer (10-min, not auto-installed); `ENABLE_MEME_NEWS_SCOUT` (default false) gates the scheduled path only; `MEME_NEWS_RETENTION_DAYS=14` | ✅ behind flag (scheduled); manual always allowed | — | Read-only scheduled discovery; `attention_score`/alerts are informational, never EV/recommendation/instruction/trade trigger; independent of and cannot affect MarketOps/EDGE-AUTO; no orders/wallets/keys/swaps/signing/execution/sizing |
| Polymarket coverage expansion | POLY-COVERAGE-001: read-only COVERAGE expansion + supply census — bounded catalog pagination (Gamma `offset`; page capped at 100), `tag_id` category + `end_date_min`/`end_date_max` resolution-window filters, bounded `/public-search` (paginates by `page`; `offset` silently ignored; nested markets inherit `endDate`/category from the parent event), deterministic Kalshi-derived targeting (whole-word title terms + prefix-anchored ticker series; **no LLM**, no external taxonomy, no new/paid provider), fair-share per-query budgets, scan provenance on `polymarket_scout_runs` (migration 0022), `polymarket-coverage-report`; new `polymarket-scan-once` options (`--limit/--orderbook-limit/--category/--active-only/--include-closed/--query/--targeted/--end-date-min/--end-date-max`) | ✅ manual always allowed (no timer; `ENABLE_POLYMARKET_SCOUT` stays false) | POLY-PRECISION-001 (shipped) — required before this may deploy | Widens WHICH public markets are observed; **identifies no arbitrage, computes no EV, recommends no trades, paper trades nothing, sizes nothing, places no orders**, uses no wallets/keys/signing/swaps/execution, and forces no match. `comparable_supply` = a comparison could be ATTEMPTED, never an opportunity. Public/no-auth GETs only, hard-bounded; skips/caps/truncation logged, never silent; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/POLY-002-matcher unchanged |
| Tick aggregation hardening | OPS-013: per-sub-window commits (seconds of lock hold; per-window stats), bounded apply+commit retry on a locked DB (re-applies work — never an empty-transaction false success), loud failed/oversized windows (audit row + nonzero exit), `tick_aggregation_runs` audit spine (migration 0024, pruned at watcher_run_days), flag-gated scheduled path (`--scheduled` no-ops unless `ENABLE_TICK_AGGREGATION_TIMER=true`, default false), timer artifacts NOT auto-installed, readiness report (coverage_72h/clean-cycles/error-free/raw-fresh gates) | ✅ manual always; scheduled behind flag + explicit timer install | Raw-retention reduction (3d→24-48h) — a separate milestone allowed ONLY when every readiness gate passes | Storage/durability only; raw ticks never deleted by aggregation; raw retention UNCHANGED; buckets are telemetry summaries, never trading signals; no EV/paper/recommendations/sizing/orders/wallets/keys/signing/swaps/execution; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket/MEME-MAS unchanged |
| Tick aggregation (storage) | OPS-012: `market_price_tick_buckets` (migration 0023) — fixed-interval OHLC/spread/liquidity summaries of raw watcher ticks; `TickAggregationService` (epoch-aligned buckets, bounded hour sub-windows with reported truncation, idempotent upsert, NULL OHLC on missing midpoints); `aggregate-market-ticks [--hours --bucket-seconds --dry-run --max-rows]` + `tick-aggregation-report` (coverage, compression, STAGED-not-enacted retention recommendation); `db-growth-report` gains heaviest tickers, steady-state projection, bucket counts, warn/crit status; buckets pruned at their own 90d window | ✅ (manual, read-only posture) | OPS-013 shipped the hardening + gated timer; raw-retention reduction remains staged behind the OPS-013 readiness gates | Operational storage/durability only — buckets are telemetry summaries, never trading signals; **aggregation never deletes raw ticks** and raw retention is unchanged; no EV, no paper trading, no recommendations, no sizing, no orders, no wallets/keys/signing/swaps/execution; watcher/MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket logic unchanged |
| Cross-venue observation windows | XVENUE-OBS-001: `xvenue-observation-report [--top]` — one-screen window verdict composing the latest persisted scan + match runs (scan provenance, clean-vs-flagged comparable split, side-uncertain/unresolved counts, mismatch reasons, overlap assessment incl. run-order warning); `docs/XVENUE_OBSERVATION_RUNBOOK.md` gives the manual sequence + measured domain/window guidance (WC/MLB/politics/crypto/tennis) | ✅ (manual, read-only) | — | Coverage intelligence for human review only — a clean comparable is a coverage fact, never an opportunity; assessments use coverage language (`insufficient_overlap`/`clean_comparable_present`…); derived on demand, no external call/persistence/migration/flag/timer/new label; no EV/arbitrage/trade/sizing/orders/wallets/keys/signing/swaps/execution; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket-scan unchanged |
| Cross-venue sample selection | XVENUE-OPS-001: recency-aware Kalshi loading for `cross-venue-match-once` (`last_seen_at DESC` — was rowid/oldest-first), tiered fallback, `--recent-hours` / `--domain` / `--market-type` sample filters, raised bounded defaults (kalshi 4000 / polymarket 500), transparent per-run sample-composition report (loaded/considered, domain + market-type breakdown, stale/no-snapshot, overlap, low-overlap note) | ✅ (manual, read-only) | — | Selection/usability only — changes WHICH persisted rows are considered, never labels/gates/precision; no forced matches; low-overlap note is observation coverage, never a signal; no EV/arbitrage/opportunity/sizing/orders/wallets/keys/signing/swaps/execution; no flag/timer/endpoint/migration/external call; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket-scan unchanged |
| Cross-venue matcher precision | POLY-PRECISION-001: side-aligned Polymarket midpoints (`resolve_poly_yes_index` / `poly_yes_midpoint`) — a midpoint and any `observed_difference` exist ONLY when the Polymarket outcome is aligned to the Kalshi YES proposition (Yes/No incl. reversed ordering, Over/Under direction, uniquely-matched named entity); raw-text over/under + handicap + `exact_score` detection (fixes "O/U 2.5" -> yes_no); compatibility gates on outcome type, market scope (player_prop/game/tournament_future), thresholds, entity-anchored scorelines, named-entity overlap and sport identity; `large_observed_difference_requires_review` REVIEW flag | ✅ (manual, read-only) | — | Semantic + midpoint CORRECTNESS only. Ambiguity degrades to `unresolved_semantic_match`, never `comparable_market_candidate`; an unaligned side yields NO midpoint and NO difference. The large-gap flag is a suspicion the MATCH is wrong — **never an opportunity, edge, arbitrage, or action**, and never rejects on its own. **No EV, no arbitrage label, no trade recommendation, no paper trading, no sizing, no orders, no wallets/keys/signing/swaps/execution**; no flag, no timer, no migration, no external call; MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket-scan unchanged |
| Cross-venue observation | POLY-002: read-only Kalshi↔Polymarket semantic matching + measurement — deterministic normalizer + `CrossVenueMatchingService` over persisted Kalshi markets/snapshots + POLY-001 polymarket markets → labels (`comparable_market_candidate`/`unresolved_semantic_match`/`incompatible_outcome`/`incompatible_resolution`/`low_confidence_match`) + measured `observed_difference` (0–1 probability midpoint gap), spreads, liquidity proxies; 2 tables (migration 0021); `cross-venue-match-once`/`cross-venue-report`/`cross-venue-candidates`; no external call, no timer | ✅ (manual, read-only) | POLY-003 (live WS, read-only) | OBSERVATION only — identifies comparable markets and measures differences; **does NOT compute EV, label arbitrage/arb, recommend trades, paper trade, size, place orders**, or use wallets/keys/signing/swaps/execution (no side/size/EV/action/order/wallet field exists); MarketOps/EDGE-AUTO/MEME-NEWS/SolanaTracker/Polymarket-scan unchanged |
| Polymarket market-data observer | POLY-001: read-only SECOND prediction-market venue — public/no-auth GETs against the Gamma market catalog + CLOB order books; `PolymarketScoutService` persists `polymarket_markets`/`polymarket_orderbook_snapshots`/`polymarket_domain_inventory_snapshots` + an audit run; `polymarket-scan-once [--scheduled]`/`polymarket-report`/`polymarket-domain-report`; `ENABLE_POLYMARKET_SCOUT` (default false) reserves loop/timer use only (manual always allowed); `POLYMARKET_RETENTION_DAYS=14`; 4 tables (migration 0020) | ✅ manual always allowed (no timer installed) | POLY-003 (live WebSocket, read-only) — POLY-002 + POLY-COVERAGE-001 shipped; no arb/EV | Read-only market-DATA observation; prices/order books are informational quotes, never EV/recommendation/instruction/trade trigger; authenticated CLOB trading endpoints deliberately NOT implemented; no API keys/wallets/keys/signing/swaps/orders/sizing/execution; cross-venue linking shipped in POLY-002; coverage broadened by POLY-COVERAGE-001 (no arb/EV/trade-candidate labels) |
| **Autonomous execution** | **Does not exist** | ❌ | Not planned | Would require every gate above plus standing human-in-the-loop controls |

| Operational observability | OPS-011: `db-growth-report` (size, table rows/MiB, tick age+domain buckets, edge/crypto growth, retention windows), retention dry-run detail, calibrated DB-growth/signal-flood alerts (configurable warning/critical) | ✅ | — | Read-only measurement/tuning only; changes no forecasting/edge/promotion logic; no EV/paper/sizing/orders/wallets/swaps/execution |

Legend: ✅ allowed now (within documented flags/limits) · 🔜 planned, not yet built · ❌ forbidden — do not implement, scaffold, or "prepare".
